import { styled } from '@mui/system';

const ColorlibConnector = styled('hr')({
  borderColor: '#C92C39', // Couleur de la ligne connectrice
  borderWidth: 2, // Épaisseur de la ligne
  borderRadius: 1, // Bordure arrondie
});

export default ColorlibConnector;
